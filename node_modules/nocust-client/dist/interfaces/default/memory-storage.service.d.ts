import { StorageManagerInterface } from '../storage-manager.interface';
export declare class MemoryStorageService implements StorageManagerInterface {
    private memory;
    get(key: string): Promise<string>;
    set(key: string, value: string): Promise<boolean>;
    del(key: string): Promise<boolean>;
}
