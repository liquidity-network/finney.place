import BigNumber from 'bignumber.js';
export declare class EonState {
    checkpointSubmitted: boolean;
    checkpointMerkleRoot: string;
    checkpointBlock: number;
    checkpointUpperbound: BigNumber;
    constructor(checkpointSubmitted: boolean, checkpointMerkleRoot: string, checkpointBlock: number, checkpointUpperbound: BigNumber);
}
