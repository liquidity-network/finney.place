import { BigNumber } from 'bignumber.js';
import { Transfer } from './transfer.model';
export interface PreparedTransfer {
    readonly transfer: Transfer;
    readonly hashes: {
        activeState: string;
        marker?: string;
    };
    readonly markerBalance: BigNumber;
}
