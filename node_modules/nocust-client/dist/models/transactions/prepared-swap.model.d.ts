import { BigNumber } from 'bignumber.js';
import { Transfer } from './transfer.model';
export interface PreparedSwap {
    readonly senderTransfer: Transfer;
    readonly recipientTransfer: Transfer;
    readonly hashes: {
        senderActiveState: string;
        recipientActiveState: string;
        recipientFulfillmentActiveState: string;
        marker?: string;
    };
    readonly balanceMarker: BigNumber;
}
export interface PreparedSwapFinalization {
    readonly hashes: {
        recipientFinalizationActiveState: string;
    };
}
export interface PreparedSwapCancellation {
    readonly hashes: {
        senderCancellationActiveState: string;
        recipientCancellationActiveState: string;
    };
}
