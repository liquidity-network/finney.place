import { Observer } from 'rxjs';
import { StateUpdatePoll } from '../models/state/state-poll.model';
import { Wallet } from '../models/wallet/wallet.model';
export declare class PollService {
    walletStatePollObserver: Observer<StateUpdatePoll>;
    private pollTimer;
    private pollTrigger;
    private pollSubscription;
    constructor(walletStatePollObserver: Observer<StateUpdatePoll>);
    startSynchronization(wallet: Wallet, initialDelay: number, period: number): void;
    stopSynchronization(): void;
    triggerSynchronization(wallet: Wallet): void;
    private subscribeToPollTrigger;
}
